import type { RegistrationType } from "@/features/registrations/types/registration.types";

export interface CertificateInterestFieldProps {
  generalPrice: number;
  memberPrice: number;
  registrationType: RegistrationType;
}
